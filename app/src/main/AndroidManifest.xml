<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.campus">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".PlangereAdminActivity"></activity>
        <activity android:name=".PlangereStudentActivity" />
        <activity android:name=".ContractIndividualActivity" />
        <activity android:name=".AnuntUserActivity" />
        <activity android:name=".AnuntAdmIndividualActivity" />
        <activity android:name=".PlangeriAdmActivity" />
        <activity android:name=".AnunturiAdmActivity" />
        <activity android:name=".InscrieriAdmActivity" />
        <activity android:name=".MancareActivity" />
        <activity android:name=".CantinaAdmActivity" />
        <activity android:name=".AdminActivity" />
        <activity android:name=".HomeActivity" />
        <activity android:name=".RegisterActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>